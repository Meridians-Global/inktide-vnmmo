{
  "name": "inktide-vnmmo",
  "version": "0.1.0",
  "private": true,
  "author": "jasonyu0100",
  "license": "UNLICENSED",
  "type": "module",
  "scripts": {
    "acquire:moon-scar": "tsx scripts/acquire-moon-scar-assets.ts",
    "acquire:spider": "tsx scripts/acquire-spider-assets.ts",
    "generate:stage-audio": "tsx scripts/generate-stage-audio.ts",
    "prepare:eyes": "tsx scripts/prepare-eye-corrections.ts",
    "recover:replicate": "tsx scripts/recover-replicate.ts",
    "build:experience": "tsx scripts/build-experience.ts",
    "dev": "vite",
    "demo": "npm run build && vite preview --host 127.0.0.1 --port 4190 --strictPort",
    "test": "node --import tsx --test tests/*.test.ts",
    "typecheck": "tsc --noEmit",
    "build": "npm run build:experience && npm run typecheck && vite build",
    "check": "npm run test && npm run build"
  },
  "dependencies": {
    "sharp": "^0.35.4",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@types/node": "^24.3.1",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2",
    "vite": "6.4.3"
  }
}
