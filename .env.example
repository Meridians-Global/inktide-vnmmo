# Builds are self-contained by default. Override only when rehearsing alternate source assets.
VN_ASSET_ROOT=.

# Offline acquisition only; the reader and normal build never need provider or Meridians access.
REPLICATE_API_TOKEN=
MERIDIANS_ROOT=/absolute/path/to/inktide
